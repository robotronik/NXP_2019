
#include "function.h"

#define SIZE 6

int main(){
	// float x[SIZE] = {5, 4, 3, 2, 1, 0};
	// int16_t y[SIZE] = {0, 25,20,15,10,5};
	// std::cout << typeid(table).name() << std::endl;
	// for(uint8_t k = 0; k<SIZE; ++k) printf("%u\n", y[k]);
	// printf("\n\n\n\n");
	// std::cout << table[0] << std::endl;

	// printf("coeff : \n");
	// float * c = ck(x, y, SIZE);
	// for(uint8_t k = 0; k<SIZE; ++k) printf("c[%u] = %f\n", k, c[k]);
	// printf("\n\n\n");
	// printf("interpolation : \n=>");
	// newton(x,y,SIZE);
	// for(uint8_t k = 0; k<SIZE; ++k) printf("%u\n", y[k]);

	// for(int k=SIZE-1; k>=0; --k) printf("%u\n", y[k]);
	return 0;
}
